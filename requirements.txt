# Core FastAPI and web framework
fastapi>=0.100.0
uvicorn[standard]>=0.20.0
python-multipart>=0.0.5
jinja2>=3.1.0

# Database
sqlalchemy>=2.0.0
alembic>=1.12.0

# HTTP client
httpx>=0.25.0

# Data validation
pydantic>=2.0.0

# Security and authentication
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4

# Monitoring and metrics
prometheus-client>=0.19.0

# Environment and configuration
python-dotenv>=1.0.0

# Logging and utilities
python-json-logger>=2.0.0

# CORS middleware (included in FastAPI but good to be explicit)
# Note: CORSMiddleware is built into FastAPI

# Optional: For production deployment
gunicorn>=21.0.0

# Development tools
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
black>=23.0.0
flake8>=6.0.0
pre-commit>=3.0.0

# For certificate verification (if using OpenSSL)
cryptography>=41.0.0

# For async database operations
asyncpg>=0.28.0  # For PostgreSQL
aiosqlite>=0.17.0  # For SQLite

# Additional utilities
click==8.1.7
rich==13.7.0  # For better CLI output

# Security scanning
bandit==1.7.5
safety==2.3.5
